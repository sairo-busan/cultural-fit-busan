# 구글시트 전체 데이터 감사 (2026-09-05, Sheets API로 전체 재확인)

정본 `[최] SAIRO 통합본`(스프레드시트 ID `1-6usA2CWMCKvUWf53SJRWwzkuvRBmwpE_H958bWAGWE`)의 **전체 28개 탭**을 Google Sheets API(`GOOGLE_SHEETS_API_KEY`, `.env.local`)로 탭 이름·gid까지 정확히 확인하고 재정리한 문서. **다시 시트를 뒤질 필요 없이 이 문서만 보면 됨.**

> 시트 자체 공지: *"단일 최신본 운영 원칙 — 이 [최] 파일만 수정합니다. v5.0 및 SAIRO_v6.0_추가탭_3종 파일은 참고·보관용입니다."*

---

## 전체 탭 목록 (28개, 실제 이름·gid 확정)

`https://docs.google.com/spreadsheets/d/1-6usA2CWMCKvUWf53SJRWwzkuvRBmwpE_H958bWAGWE/edit?gid={GID}`

| idx | gid | 탭 이름 | 이 문서 섹션 |
|---|---|---|---|
| 0 | 1527861096 | 90_V5작성규칙 [숨김] | §19 (참고, 생략) |
| 1 | 1695359623 | 91_CFP16유형 [숨김] | §19 (참고, 생략) |
| 2 | 1110621756 | 00_관리안내 | §11 |
| 3 | 1069717930 | 03A-1_CF설문3문항 | §7 |
| 4 | 460297482 | 03A-3_여행경험설문 | §7 |
| 5 | 1463024457 | 03A-4_평가기준·근거 | §6 |
| 6 | **1126787505** | **예시_CF8추천구조** | §2 |
| 7 | 252207444 | 03A_CF8프로필 | §17 |
| 8 | 11755224 | 03A-2_CF점수기준 | §8 |
| 9 | 2138938687 | 03B_추천시나리오 | §9 |
| 10 | 574723462 | 03C_상황트리거 | §9 |
| 11 | 1808300401 | 04_CF8_유형 | §16 |
| 12 | 1808300501 | 05_CFQ_취향문항 | §18 |
| 13 | 1808300601 | 06_TRV_여행경험문항 | §14 |
| 14 | 1808300701 | 07_UI_화면수정 | §14 |
| 15 | 1808300801 | 08_UXF_유저플로우 | §14 |
| 16 | 1383299567 | 93_V5진행현황 [숨김] | §19 (참고, 생략) |
| 17 | 111239497 | 01_장소마스터 | §10 |
| 18 | 555968524 | 01_장소마스터_수정 | §10 |
| 19 | 1120133669 | 02_검수대상 | §20 |
| 20 | 250552143 | 03_추천시나리오 | §9 |
| 21 | **1087571184** | **04_추천로직** | **§15 (신규 발견)** |
| 22 | 687393444 | 05_API·화면연결 | §13 |
| 23 | 350815285 | 92_V5상세태깅60 | §1 |
| 24 | 930229695 | 99_코드사전 | §12 |
| 25 | 1089184996 | 10_속성사전_v1 | §5 |
| 26 | **274894934** | **11_장소점수_62** | §3 |
| 27 | 319874358 | 12_CF8매칭검증 | §4 |

---

## 0. 요약 — 지금 뭐가 급하고 뭐가 풀렸는지

**아직 유나 확인 대기중 (카톡 발송됨)**:
1. `92_V5상세태깅60`에 `place_id` 추가
2. `92_V5상세태깅60`에 `pet_allowed`/`wheelchair_accessible`/`stroller_accessible`/`stairs_alternative` 빈 컬럼 추가
3. `예시_CF8추천구조`(gid=1126787505)를 62건(6개 권역 전체) 실데이터로 채우기 — 지금 13건(해운대·센텀만)

**이번 재조사로 직접 확인/해결됨**:
- `01_장소마스터` vs `01_장소마스터_수정` — **결론 남, §10 참고**(유나 답 안 기다려도 됨)
- `04_추천로직` 탭 새로 발견 — 장소마스터 큐레이션 규칙 30개(R001~R070). 우리 스코프(92_V5상세태깅60 62건 안에서만 추천)엔 대부분 무관하지만, **R016(검수상태 게이트)·R024(trip_setup_mode 정의)·R031(UNKNOWN 재정규화)** 3개는 직접 관련 — §15 참고
- `04_CF8_유형` 탭에서 CF8 8유형 ↔ 구 CFP16 정확한 매핑표 확보 — §16
- CF8 3축·코스역할 7종은 `11_장소점수_62`(구 "확장 태깅 후보")에서 그대로 가져오면 됨(유나 요청 불필요) — §3

---

## 1. 92_V5상세태깅60 (gid=350815285) — 이미 임포트 완료

62행. `content_id` 있는 49건만 `placeTags` 컬렉션에 적재 완료(`scripts/import-place-tags.ts`). 13건은 SAIRO 자체 큐레이션이라 content_id 없음 → 현재 스킵 중(place_id 추가되면 이 13건도 살릴 수 있음).

**필드**: noise_level, crowd_level, crowd_peak, crowd_calm, local_depth, english_support, spice_level, weather_type, best_time, fit_solo, tip_type, tip_headline_ko, pro_ko, con_ko, why_ko, why_en, coverage, cfp_match, has_raw, has_meat_only, has_seafood_only, seating_type, fit_couple, fit_friends, fit_family, stay_minutes, budget_level, pro_en, con_en, info_ko, info_en, source_url, status, alt_id_1/2, tagged_by, manual_locked, kto_title_ko, kto_addr1, kto_modifiedtime, kto_image_license, api_verified_at, api_match_status (총 56컬럼)

**DB**: `placeTags` (이미 적재됨) — **로직 필요**: 그대로 사용 중.

---

## 2. 예시_CF8추천구조 (gid=1126787505) — 13행 샘플만 존재, 62건 실데이터 필요

**탭 이름 자체가 "예시"** — 이름부터 실데이터가 아니라 계산 구조 검증용 샘플이라는 뜻.

실제 파일 구조: 위쪽 3줄이 범례/안내, 4번째 줄이 진짜 헤더.

```
행1: 샘플 사용자,EFV,동행,혼자,날씨,SUNNY,계절,봄,시간대,저녁,... (표 전체가 "혼자·SUNNY·봄·저녁·EFV" 사용자 1명 기준 계산 예시)
행2: 안내,계산 구조 검증용 샘플입니다. 장소 축 점수는 03A-2_CF점수기준에 따라 재검수 후 확정합니다.
행4(헤더): place_id, 장소명, 권역, service_status, 관광가치점수,
           cf_atmosphere, cf_local_famous, cf_deep_variety,          ← 장소 고정 CF8 3축
           혼자,연인과,친구와,부모님과,아이와,반려동물과(6종),         ← 동행유형별 점수
           SUNNY,RAINY,CLOUDY, 봄,여름,가을,겨울, 오전,오후,저녁,
           CF8_score, 선택_동행점수, 선택_날씨점수, 선택_계절점수,
           선택_시간점수, 최종점수, eligible, rank, 추천이유, scoring_status
행5~17(13건): plc_로 시작하는 place_id로 row 식별
```

**⚠️ 이 탭은 `content_id`가 아니라 `place_id`로 장소를 식별한다.** 92_V5상세태깅60(`content_id` 키)과 조인 키가 다름 — 92번에 `place_id`를 추가해달라는 요청의 직접적인 이유.

**현재 상태**: 13건 전부 **해운대·센텀 권역뿐**. `scoring_status = SAMPLE_NEEDS_REVIEW`.

**DB**: `혼자~반려동물과`(6종)·날씨3·계절4·시간대3 → `placeTags`에 필드 추가. `CF8_score~scoring_status`는 요청마다 계산, 저장 안 함.

**로직 필요**: 최종점수 계산에 그대로 씀. **62건(6개 권역 전체) 채워지기 전까지는 추천엔진 실동작 불가** — 가장 급한 요청.

---

## 3. 11_장소점수_62 (gid=274894934, 62건 전체 존재)

92번과 별개 탭, `content_id`로 조인(92번과 동일 키). 실제 헤더:

```
순번, content_id, 장소명, 권역, 카테고리,
분위기축(-2~2), 로컬↔대표축(-2~2), 깊게↔다양축(-2~2),      ← CF8 3축 (여기 실데이터 있음!)
사진·기억, 문화, 자연, 음식, 걷기필요, 휴식가능, 실내차양(0~100 각),  ← 코스역할 7종
날것, 육류만, 해산물중심,                                    ← ⚠️ 기존 has_raw 등과 이름은 같지만 별도 계산값, 값 일치 여부 미확인
반려동물, 휠체어, 유아차, 계단대체,                          ← 62건 전부 UNKNOWN
근거등급, 근거요약, 신뢰도, 검수상태
```

실제 1번째 데이터 행(해운대해수욕장, content_id=126081):
```
1,126081,해운대해수욕장,해운대,해양형,1,1,2,50,50,75,75,25,75,0,
UNKNOWN,UNKNOWN,UNKNOWN,UNKNOWN,UNKNOWN,UNKNOWN,UNKNOWN,
C_EXISTING_TEXT+PROXY,"noise_level=4; local_depth=2; stay_minutes=20; category=해양형",0.55,NEEDS_REVIEW
```
→ `근거요약` 컬럼이 "이 값을 92번의 어떤 필드로 어떻게 계산했는지"를 행마다 직접 적어놓음.

**중요**: CF8 3축·코스역할 값은 사람이 새로 조사한 게 아니라 기존 필드를 임시 변환한 값(`10_속성사전_v1`에 변환 규칙 명시, §5 참고):
- `cf_atmosphere` ← `noise_level`(1~5) 변환
- `cf_local_famous` ← `local_depth`(1~5) 반대방향 변환
- `cf_deep_variety` ← `stay_minutes` 구간 변환
- 코스역할 7종 ← 이름·카테고리·설명 키워드 추론(신뢰도 0.55~0.65, `NEEDS_REVIEW`)

반려동물/휠체어/유아차/계단대체는 이 탭에서도 **62건 전부 UNKNOWN** → 데이터 자체가 시트 어디에도 없다는 걸 재확인.

**DB**: CF8 3축·코스역할 7종 → `placeTags`에 필드로 추가, **탭 값 그대로 content_id로 임포트**(유나 요청 불필요). 반려동물 등 4종 → 요청 필요.

**로직 필요**: CF8 3축은 최종점수 계산 필수. 코스역할 7종은 지금 순위 계산엔 안 쓰지만 나중에 "코스 짜기" 기능용으로 필드만 미리 확보.

---

## 4. 12_CF8매칭검증 (gid=319874358, 62건) — 계산 검증용, 프로덕션 데이터 아님

`content_id`로 조인. 62건 전체 존재.

헤더: `순번, content_id, 장소명, 분위기축, 로컬↔대표축, 깊게↔다양축, CLD,CLV,CFD,CFV,ELD,ELV,EFD,EFV(8유형 점수), 최고유형, 최고점수, 기존 cfp_match, 기존값 일치, 점수 근거, 검수상태, 계산 버전, 주의사항, 개발 사용, 비고`

- 점수 근거 컬럼에 공식 명시: **"3개 CF축 평균(축별 50+25×사용자축×장소축)"** — `03A-2_CF점수기준`(§8)과 동일 공식.
- "개발 사용" 컬럼에 "개발용 계산 검증"이라고 명시 — **임포트 대상 아니라 공식 검증용 참고자료**.
- 누리마루 APEC하우스 등 `기존값 일치` 컬럼에서 구 CFP16 값과 신 CF8 값 매칭 여부 체크 흔적(이전에 "데이터 오류"로 오판했던 게 이 표 덕분에 정정됨).

**DB**: 임포트 안 함. **로직 필요**: 공식 재확인용으로만 참고 완료.

---

## 5. 10_속성사전_v1 (gid=1089184996) — ★가장 중요한 참조표★

전체 추천엔진 관련 필드의 **공식 상태(있음/없음/계산가능)를 한눈에 알려주는 표**. 컬럼: `attribute_id, 속성군, 화면표시명, 정의, data_type, 허용값/척도, 최저/중간/최고 기준, 현재 산출 방식, 필터 여부, UNKNOWN 처리, 앱 적용 위치, 근거 요구, 버전, 상태`

| attribute_id | 상태 | 현재 산출 방식 | 결론 |
|---|---|---|---|
| `cf_atmosphere_score` | INFERRED | noise_level 1~5 임시 변환 | `11_장소점수_62`에서 임포트 |
| `cf_local_famous_score` | INFERRED | local_depth 1~5 반대방향 변환 | 〃 |
| `cf_deep_variety_score` | INFERRED | stay_minutes 구간 변환 | 〃 |
| `photo_memory_value`/`cultural_value`/`nature_value`/`food_value` | INFERRED | 이름·카테고리·설명 키워드 추론 | 〃 |
| `walking_required` | INFERRED | stay_minutes 구간 변환 | 〃 |
| `rest_availability` | INFERRED | 설명 키워드 추론 | 〃 |
| `indoor_shelter` | INFERRED | weather_type을 0/50/100 변환 | 〃 |
| `service_active` | **NEEDS_DATA** | 공식 정보 확인값만 사용, 현재 없음 | TourAPI 실시간 호출로 대체 |
| `open_at_visit` | **NEEDS_API** | 실시간/운영시간 API 연결 필요 | TourAPI 실시간 호출로 대체 |
| `pet_allowed` | **NEEDS_DATA** | "현재 62곳 모두 UNKNOWN" 명시 | **92번에 빈 필드 추가 요청 필요** |
| `wheelchair_accessible` | **NEEDS_DATA** | "현재 62곳 모두 UNKNOWN" 명시 | **92번에 빈 필드 추가 요청 필요** |
| `stroller_accessible` | **NEEDS_DATA** | "현재 62곳 모두 UNKNOWN" 명시 | **92번에 빈 필드 추가 요청 필요** |
| `stairs_alternative` | **NEEDS_DATA** | "현재 62곳 모두 UNKNOWN" 명시 | **92번에 빈 필드 추가 요청 필요** |
| `has_raw`/`has_meat_only`/`has_seafood_only` | MIXED_REVIEW | 기존 태깅값 이관 | 이미 placeTags에 있음 |
| `companion_fit` | DERIVED_ONLY | "사실값처럼 저장하지 않는다" 명시 | 저장 금지, 매 요청 계산만 |

**결론**: 접근성/반려동물 필드는 "내가 못 찾은 것"이 아니라 "시트 자체가 아직 없다고 공식 선언한 것". 헌팅 불필요.

---

## 6. 03A-4_평가기준·근거 (gid=1463024457) — 하드필터·척도 정의서

두 종류 정의가 섞여 있음:

**a) VALUE_SCALE**: VAL_100/75/50/25/0/UNKNOWN — 모든 ORDINAL 점수에 공통 적용되는 100/75/50/25/0 척도와 "UNKNOWN은 공란, 절대 0과 혼용 금지" 규칙.

**b) HARD_FILTER**: `HF_SERVICE(service_active)`, `HF_OPEN(open_at_visit)`, `HF_SOLO(solo_entry_allowed)`, `HF_PET(pet_allowed)`, `HF_CHILD(child_age_allowed)`, `HF_STROLLER`, `HF_WHEELCHAIR`, `HF_STAIRS` — 전부 TRI_STATE(1/0/UNKNOWN), "0이면 제외·UNKNOWN이면 확인 필요 표시" 원칙 공통.

- `COMF_01(physical_burden)`: MODE_SCORE — "보행거리·경사·계단 부담" 종합 점수.
- `HUMAN_01(verify_hard_filters)`: "반려동물·아동·접근성·운영정보를 검수 전 자동추천 금지".

**확인 필요**: `HF_SOLO`(solo_entry_allowed), `HF_CHILD`(child_age_allowed)는 아직 placeTags에 없는 신규 하드필터 후보 — 온보딩 문항엔 KID01/PET01/MOVE01만 있고 SOLO 관련 질문은 없음. 태무·유나 확인 필요.

---

## 7. 03A-1_CF설문3문항 (gid=1069717930) + 03A-3_여행경험설문 (gid=460297482)

```
question_id, axis_field, 질문, left_code, left_label, left_description,
right_code, right_label, right_description, response_value, independence_note, status
```

- `CFQ01`(cf_atmosphere, C/E), `CFQ02`(cf_local_famous, L/F), `CFQ03`(cf_deep_variety, D/V) — CF8 3문항 원문. `response_value: 왼쪽=-1/오른쪽=+1`.
- `KID01`(child_age_group), `PET01`(with_pet), `MOVE01`(mobility_constraints, 복수선택: STAIRS/STROLLER/WHEELCHAIR — **"계단이 어렵다고 하셨어요" 화면 문구의 원본 질문**)

**DB**: 질문 자체는 저장 안 함(클라이언트 상수). `mobility_constraints` 선택값은 서버로 안 보냄(위치정보 원칙과 동일).

**로직 필요**: `MOVE01` 응답값이 곧 `stairs_alternative`/`stroller_accessible`/`wheelchair_accessible` 하드필터와 매칭 — 데이터 없으니 "UNKNOWN → 확인 필요 표시"로만 처리.

---

## 8. 03A-2_CF점수기준 (gid=11755224) — 계산식 정의서

```
TAG_A_-2~2 (cf_atmosphere_score 태깅 기준 5단계)
TAG_DV_-2~2 (cf_deep_variety_score 태깅 기준 5단계)
CALC_01 user_cf_axis — 3문항 좌우선택을 축별 -1~+1로 저장
CALC_02 axis_match_score — 50+25×user_axis×place_axis (0~100)
CALC_03 cf_fit_score — AVERAGE(3축 일치도) = Y값(CF8_score)
CALC_04 final_score — CF 35% + 동행 25% + 날씨 15% + 계절 10% + 시간대 15%  [DRAFT]
CALC_05 hard_filter — HIDDEN·운영불가·필수제약 먼저 제외
CALC_06 service_status — 노출등급 동점 보정/소폭 보너스, "고정값으로 사용 금지"
```

**회귀분석으로 독립 도출한 공식(9/13 exact, 4/13 rounding 오차)과 완전히 일치 확인됨.** `CALC_04`는 "DRAFT" 상태.

**⚠️ §15의 R031(UNKNOWN 재정규화)과 충돌 가능성**: CALC_04는 고정 가중치(35/25/15/10/15)인데, R031은 "UNKNOWN인 축은 제외하고 나머지로 재정규화"하라고 함. 지금 companionScore·weatherScore 등이 전부 null인 상태라 이 둘을 같이 봐야 함 — §15 참고.

**확인 필요**: CALC_04가 DRAFT라 최종 확정본인지, CALC_06(노출등급 보정) 구현 필요 여부.

---

## 9. 03B_추천시나리오 (gid=2138938687) + 03C_상황트리거 (gid=574723462) + 03_추천시나리오(구, gid=250552143)

**03B_추천시나리오** — 상황별 시나리오 정의:
```
scenario_name, scenario_group, engine_role, Trigger, User, Context, Need,
Recommendation Object, Content Mix, Output, CF8 적용, 필수데이터·API,
구현가능성, 결합·충돌 규칙, fallback, status
```

7개 시나리오, 전부 `status: DRAFT_READY`(설계 완료, 미구현):

| 시나리오 | 트리거 | 구현가능성 | 비고 |
|---|---|---|---|
| CTX_NOW_3H | 남은시간 ≤180분 | MEDIUM | 마이크로 코스 |
| CTX_NEARBY | 위치권한+주변추천 선택 | HIGH | W2 예정 기능(S40)과 동일 개념 |
| CTX_RAIN | 우천 | MEDIUM | 실내 우선 재정렬 |
| CTX_NIGHT | 18시 이후 | MEDIUM | 야간 코스 |
| CTX_PET | 반려동물 모드 | MEDIUM | 반려동물 API 아직 없음 |
| CTX_ACCESS | 무장애 모드 | MEDIUM | 무장애 API 아직 없음, "최우선 hard filter" 명시 |
| CTX_FAMILY | 부모님·시니어 동행 | MEDIUM | 무장애정보 의존 |

**03C_상황트리거**(`TRG_PET`/`TRG_FAMILY` 등) — 트리거 조건 상세(EQ pet_mode=Y 등) + 우선순위.

**03_추천시나리오(구버전, gid=250552143)** — 03B와 다른 형식(`product_type, title_ko, target, trigger, place_sequence, required_tags, preferred_roles, hard_filters, data_sources, ranking_emphasis, explanation_template, replacement_rule, screen, launch_priority`). "코스 상품" 단위 정의로 보이며 03B보다 이전/다른 버전 — 03B가 최신으로 판단(탭 인덱스 순서상 03B가 뒤에 위치, 명명 규칙도 최신 계열과 일치).

**결론**: 전부 W1 스코프 밖(설계만 있고 미구현), CTX_PET·CTX_ACCESS는 데이터 자체가 없어 MVP2 이후로 미루는 게 타당.

**DB**: 시나리오 자체는 저장 안 함, 로직 상수로 구현 예정(미착수).

---

## 10. 01_장소마스터 (gid=111239497) vs 01_장소마스터_수정 (gid=555968524) — ✅ 이번에 직접 비교해서 결론 남

둘 다 2,252행. 같은 place(예: 해운대해수욕장, content_id=126081)를 직접 대조:

**01_장소마스터(원본)** — 30컬럼, 실제 데이터:
```
plc_0d9413d7..., 해운대해수욕장, kto:ko:126081, 126081, attraction, 9(언어수), "ko,en,ja,zh-CN,...",
Y, 217.36, 9, coordinate_spread, ACTIVE, STANDARD, ANCHOR, feed_course_docent, (제외사유없음),
"바다,해운대,해양형", any, outdoor, EFV, Y, 100(quality_score), LOW, "V5 사람 태깅 후보와 일치",
129.16, 35.16, 74(tourism_value_score), NEEDS_REVIEW, Y, N, COURSE_SUPPORT
```

**01_장소마스터_수정** — 16컬럼으로 축소, 같은 place:
```
해운대·센텀, plc_0d9413d7..., 해운대해수욕장, 126081, 관광지, "2순위 추천"(=STANDARD를 한글 라벨로),
ANCHOR, feed_course_docent, EFV, any, outdoor, 74, NEEDS_REVIEW,
"여행구조·날씨·시간·CF8 조건에 따라 추천", 129.16, 35.16
```

**결론: "_수정"은 원본의 업데이트판이 아니라, 원본에서 일부 컬럼만 뽑고 한글 라벨을 입힌 파생/열람용 뷰다.** place_id·content_id·quality 수치(74)는 완전히 동일 — 데이터 자체는 안 바뀜. 다만 `_수정` 쪽은 `language_count`/`languages`/`has_ko`/`coordinate_spread_m`/`quality_score`/`review_priority`/`exclusion_reason`/`foreigner_ready`/`event_dependency`/`exposure_reason_code` 등 **엔지니어링에 중요한 컬럼을 다 뺐음** — 기획/열람용으로 보임.

**→ `placeMaster`를 나중에 만들게 되면 원본(gid=111239497)을 소스로 써야 함.** 지금 스코프(92_V5상세태깅60 62건 안에서만 추천)엔 어느 쪽도 당장 안 씀. 카톡으로 유나 확인 요청은 유지해도 되지만(공식적으로 어느 쪽이 최종본인지는 유나 의도 확인이 맞음), 엔지니어링 판단으로는 이미 결론 남.

---

## 11. 00_관리안내 (gid=1110621756)

"SAIRO 콘텐츠 데이터 마스터 운영 요약" — 마스터 데이터 파이프라인의 관리 원칙을 설명하는 문서형 탭(표 형식 아님). 목적: "관광공사 통합 DB와 V5.0 태깅을 결합해 피드·장소상세·맞춤코스에 바로 쓸 수 있는 운영 기준을 만든다." 이전 대화에서 스크린샷으로 확인 완료.

---

## 12. 99_코드사전 (gid=930229695) — 노출등급 정의 + 지역/카테고리 코드 샘플이 한 표에 뒤섞여 있음

컬럼이 서로 무관한 코드북 여러 개를 나란히 붙여놓은 구조:

```
EXPOSURE: FEATURED(75점+) / STANDARD(50~74) / SEARCH_ONLY(30~49 또는 이벤트·편의형) / CONDITIONAL(조건부) / HIDDEN(29점 이하 또는 하드제외)
REVIEW: VERIFIED / NEEDS_REVIEW(02_검수대상 연결) / REJECTED
FOREIGNER: Y(언어 2개+) / PARTIAL(한국어만) / N(정보부족)
EVENT: N(상설) / Y(공연·상영 일정 의존, SEARCH_ONLY 기본)
REASON: TOURISM_CORE(FEATURED) / COURSE_SUPPORT(STANDARD) / EVENT_DEPENDENT(SEARCH_ONLY) / HARD_EXCLUDED_RETAIL(HIDDEN) / LOW_TOURISM_RELEVANCE(HIDDEN)
```

같은 표 왼쪽에 area/category/content_type_id 코드 샘플, `legacy_cf16`(QLMS 등)·`cf8`(CLD 등) 나란히 있음 — CF8과 무관한 레거시 컬럼은 무시.

**DB**: `placeMaster`(현재 안 만듦) 참고용. **로직 필요**: 지금 당장 필요 없음(62건 전부 coverage 70+ 정상노출이라 노출등급 필터가 실질적으로 안 걸림).

---

## 13. 05_API·화면연결 (gid=687393444) — 특화 API 활용 계획표

```
구분, 특화 API, 연결 단위, 연결 키, 핵심 원천 데이터,
SAIRO 생성 필드, 앱 화면, 활용 서비스, 갱신주기, 우선순위, 구현상태, 담당, 검증·Fallback
```

| 특화 API | 우선순위 | 구현상태 | 비고 |
|---|---|---|---|
| KTO 통합 장소마스터 | MVP1 | **운영중** | 기본 데이터, 이미 사용 중 |
| **무장애 여행 정보** | MVP2 | **설계안**(미구현) | "미확인은 UNKNOWN, 접근 가능으로 추정 금지" |
| **반려동물 동반여행 서비스** | MVP2 | **설계안**(미구현) | "확인된 조건만 노출, 미확인은 추천 제외" |
| 의료관광정보 | MVP3 | 보류 | 법무 검토 필요 |
| 웰니스관광정보 | MVP2 | 설계안 | "의료효과 표현 금지" |
| 고캠핑 정보 | MVP3 | 설계안 | 예약 가능 단정 금지 |

**결론**: 접근성·반려동물 전용 API가 공식적으로 아직 설계 단계 — MVP2 이후 항목으로 미루되, 필드 스키마는 지금 만들어야 함.

---

## 14. 06_TRV_여행경험문항 (gid=1808300601) + 07_UI_화면수정 (gid=1808300701) + 08_UXF_유저플로우 (gid=1808300801)

`TRV01~06`(여행경험문항, 소피의 `types/trip.ts` 원본), `S03-1~4`(신규 온보딩 화면), `S20/S30`(장소상세·방문전확인), `UF01/UF04`(사용자 흐름).

핵심 UI 원칙(여러 화면에 반복 명시):
- **S20**: "접근·음식 정보 미확인은 '불가'가 아니라 '정보 확인 중'"
- **S30**: "추정치·미확인 정보로 차단 금지 · 확인된 이동·반려동물·아이·음식 충돌만 노출"
- **UF04**: "미확인 정보는 정보 확인 중"

화면은 UNKNOWN 상태를 이미 정상 케이스로 취급하도록 설계돼 있음 — 데이터가 비어 있어도(null) 앱은 정상 작동하게 이미 기획됨.

**DB**: 이 화면들이 참조하는 필드(`stairsAlternative` 등)는 placeTags에 null로 존재하면 됨.

**로직 필요**: 프론트에서 `null`을 "정보 확인 중"으로 렌더링 — 소피 쪽 UI 구현 시 참고.

---

## 15. 04_추천로직 (gid=1087571184) — ⚠️ 이번에 새로 발견, 규칙 30개(R001~R070)

**정체**: 장소마스터(2,252건) 원본을 큐레이션해서 노출등급을 매기는 파이프라인 규칙. 92_V5상세태깅60(62건)은 이미 사람이 이 파이프라인을 거쳐 선별한 결과물이라, **대부분의 규칙은 지금 스코프에 다시 적용할 필요 없음.** 그래도 3개는 직접 관련:

| rule_id | 내용 | 우리 스코프 관련성 |
|---|---|---|
| **R016** | 검수상태 분리 — `review_status`가 `NEEDS_REVIEW`/`REJECTED`면 노출등급과 별도로 게시 게이트 적용(`publish_eligible=false`) | ⚠️ **92_V5상세태깅60의 62건 중 검수상태 확인 필요** — 아직 REVIEW 안 끝난 게 섞여있으면 추천에서 빼야 함 |
| **R024** | 추천 입력 모드 분기 — `trip_setup_mode = QUICK 또는 CUSTOM`. QUICK은 CF8+자동상황만 적용, CUSTOM은 확인된 여행 옵션(동행·이동제약·음식제약 등)을 추가 적용. "미응답은 '특별히 없음'이 아니며 하드필터를 만들지 않음" | ✅ **이게 PR#10에서 소피한테 물어봤던 `trip_setup_mode`의 정의** — QUICK/CUSTOM 2모드 분기가 답. S02에서 S10 직행(QUICK) 또는 옵션 설정 후 S10(CUSTOM) |
| **R031** | UNKNOWN·미설정 재분배 — 축 값이 UNKNOWN이거나 옵션 미설정(null/[])이면 **해당 축을 제외하고 나머지로 가중치 재정규화**. "NONE 값으로 대체 금지" | ⚠️ **CALC_04(§8)의 고정 가중치 35/25/15/10/15와 충돌 가능** — 지금 companionScore·weatherScore 등이 전부 null이라, 고정 가중치 그대로 쓰면 안 되고 null인 축은 빼고 나머지 비율로 재정규화해야 정확함(예: 동행·날씨·계절·시간대가 전부 null이면 CF매치 100%로 계산) |

그 외 규칙 요약(참고용, 지금 스코프 밖):
- **Hard Filter류(R010~R019)**: 비활성 제외, 일반 소매점 제외, 검증된 음식 제약(이미 구현됨), 좌표 품질 게이트 등 — 2,252건→92건 큐레이션에 이미 반영된 걸로 간주
- **후보생성(R020~R024)**: 내 주변(R021, 위치기반, 향후 기능), 연관 장소(R022, PLANNED), 노출등급 결정(R023: FEATURED≥75, STANDARD≥50, SEARCH_ONLY≥30 — `tourism_value_score` 기준, `AD 최종점수`와는 다른 별개 점수)
- **점수(R030~R032)**: `tourism_value_score` 산출식("여행목적성25+부산고유성20+경험20+코스15+이용가능10+수요10") — 이건 **장소의 객관적 매력도 점수**고, 우리가 만드는 AD 최종점수는 **사용자별 개인화 매칭 점수**라 서로 다른 개념. 헷갈리지 않게 구분 필요
- **코스(R040~R043)**: 코스 짜기 기능(역할 조합, 운영시간 정렬, 이동 상한, 리듬 균형) — W2 이후
- **실시간(R050~R051)**: 예상 혼잡도·날씨 대체 코스 — PLANNED
- **설명/운영(R060, R070)**: 추천 이유 문장 생성(이미 구현 방향과 일치), 수동 잠금(manual_locked=Y는 자동 덮어쓰기 금지)

**확인 필요**: R016 검수상태 게이트를 92_V5상세태깅60 62건에 적용해서 실제로 걸리는 게 있는지 확인 필요(placeTags에 검수상태 필드 자체가 없어서 지금은 체크 불가 — 92번에 이 필드도 있는지 봐야 함).

---

## 16. 04_CF8_유형 (gid=1808300401) — CF8 8유형 ↔ 구 CFP16 정확한 매핑

```
type_code, type_name_ko, type_name_en, axis_atmosphere, axis_place, axis_pace,
one_line_ko, guide_1, guide_2, guide_3, legacy_cf16, status
```

| CF8 | 이름 | legacy_cf16(구 4자리 코드, 콤마 = 축소 매핑) |
|---|---|---|
| CLD | 조용한 골목 산책자 | QLMS, QLHS |
| CLV | 조용한 로컬 탐험가 | QLMW, QLHW |
| CFD | 느긋한 뷰 감상자 | QFMS, QFHS |
| CFV | 조용한 명소 수집가 | QFMW, QFHW |
| ELD | 시장 골목 정착자 | VLMS, VLHS |
| ELV | 활기찬 로컬 러너 | VLMW, VLHW |
| EFD | 핫플 여유 감상자 | VFMS, VFHS |
| EFV | 인기 명소 순회자 | VFMW, VFHW |

각주: *"legacy_cf16 = 기존 CF16 코드. 음식축(M/H) 제거 후 3자리로 축약한 매핑입니다. 01_places의 cfp_match 컬럼도 같은 규칙으로 4자리→3자리 변환이 필요합니다."*

**로직 필요**: 92_V5상세태깅60의 `cfp_match` 컬럼 값(이미 EFV처럼 3자리로 들어있는 것도, ELMW처럼 4자리로 남아있는 것도 섞여있을 가능성) — 임포트 전에 전부 3자리 CF8 형식인지 확인 필요. 지금까지 본 샘플들은 이미 3자리였음(정상).

---

## 17. 03A_CF8프로필 (gid=252207444)

```
cf_code, profile_name, 분위기축, 장소축, 경험축, axis_vector, preferred_tags,
ranking_emphasis, explanation_template, survey_result_rule, behavior_update_policy, status
```

CF8 8유형별 프로필 정의(추천 이유 문장 템플릿, 태그 선호도 등) — S02 결과 화면·S20 추천이유 생성에 참고할 원천. 04_CF8_유형과 내용 겹치는 부분 있음(같은 8유형, 다른 관점의 정의).

---

## 18. 05_CFQ_취향문항 (gid=1808300501)

```
question_id, axis, screen_order, question, helper_text, left_code, left_label,
left_description, right_code, right_label, right_description, status
```

03A-1_CF설문3문항(§7)의 **화면 표시용 축약 버전**(문구가 더 짧고 UI 친화적). 03A-1이 "정의서", 이건 "화면에 실제 띄우는 문구"로 역할이 나뉜 것으로 보임 — 소피가 실제 구현 시 이 탭 문구를 썼을 가능성 높음, 필요 시 03A-1과 대조.

---

## 19. 90_V5작성규칙/91_CFP16유형/93_V5진행현황 (전부 숨김 탭) — 참고용, 엔진 로직과 무관

- **90_V5작성규칙**: "Cultural Fit Busan — 장소 태깅 시트 v5" 작성 가이드라인(유나가 태깅할 때 보는 매뉴얼). 우리 스키마엔 영향 없음.
- **91_CFP16유형**: 구 CFP16 16유형 전체 정의(QLMS 등). CF8 전환 전 레거시, 확인용으로만 남아있음.
- **93_V5진행현황**: 권역별 태깅 진행률 트래커(해운대 10/10 등). 프로젝트 관리용, 엔진 로직과 무관.

---

## 20. 02_검수대상 (gid=1120133669)

```
place_id, canonical_title, place_category, service_status, review_status,
tourism_value_score, review_priority, exclusion_reason, decision_basis,
exposure_reason_code, mapx, mapy
```

74행 — `01_장소마스터`(2,252건) 중 `review_status=NEEDS_REVIEW`인 73건 + `STANDARD` 1건을 뽑아놓은 검수 대기 큐. 04_추천로직 R016이 참조하는 바로 그 리스트. 92_V5상세태깅60 62건과 겹치는 항목이 있는지는 미확인 — 겹치면 §15의 R016 적용 대상.

---

## 21. 최종 DB 매핑 표 (실행 요약)

| 필드 | 시트 상태 | DB 위치 | 값 확보 방법 |
|---|---|---|---|
| noise_level~alternativeIds 등 32종 | 92_V5상세태깅60, 있음(49건) | `placeTags` | 이미 임포트 완료 |
| cf_atmosphere/cf_local_famous/cf_deep_variety | 11_장소점수_62, 있음(62건, INFERRED) | `placeTags` 필드 추가 | **탭 값 그대로 content_id로 임포트** |
| 사진기억/문화/자연/음식/걷기필요/휴식가능/실내차양 | 11_장소점수_62, 있음(62건, INFERRED) | `placeTags` 필드 추가 | **탭 값 그대로 content_id로 임포트** |
| 혼자~반려동물과/SUNNY~CLOUDY/봄~겨울/오전~저녁 | 예시_CF8추천구조, **13건뿐(해운대·센텀만), place_id 키** | `placeTags` 필드 추가 | **유나 요청 필요**(카톡 3번) |
| pet_allowed/wheelchair_accessible/stroller_accessible/stairs_alternative | 시트 전체에 **0건** | `placeTags` 필드 추가(null 기본) | **유나 요청 필요**(카톡 2번) |
| place_id | 01_장소마스터엔 있음, 92번엔 없음 | `placeTags` PK 후보 | **유나 요청 필요**(카톡 1번) |
| solo_entry_allowed/child_age_allowed | 하드필터 정의서엔 있으나 대응 온보딩 문항 없음 | 미정 | **확인 필요** |
| CF8_score~scoring_status(Y~AH) | 계산값, 저장 대상 아님 | `recommendationSnapshots`(신규) | 요청 시 계산 |
| tourism_value_score(노출등급용) | 마스터에 있음, 92번엔 없음 | 미사용 | 62건 전부 이미 노출승인된 상태라 불필요 |
| review_status(검수 게이트, R016) | 마스터/02_검수대상엔 있음, 92번엔 없음 | **확인 필요** | 62건 중 REVIEW 안 끝난 게 있는지 확인 |
| trip_setup_mode | 04_추천로직 R024에 정의됨 | 저장 안 함(요청 파라미터) | ✅ QUICK/CUSTOM 2모드로 확정(§15) |

---

## 22. 확인이 여전히 필요한 것

- [ ] `예시_CF8추천구조` 62건 실데이터 (카톡 발송됨) — **가장 급함**
- [ ] `pet_allowed` 등 4종 92번 컬럼 추가 (카톡 발송됨)
- [ ] `place_id` 92번 컬럼 추가 (카톡 발송됨)
- [x] `01_장소마스터` vs `_수정` — 엔지니어링 결론 남(§10), 카톡은 유나 의도 확인 차원에서 유지
- [ ] `CALC_04`(35/25/15/10/15) 확정 여부 + R031(UNKNOWN 재정규화)과의 관계 정리 — 다음 단계(최종점수 계산 로직) 구현 시 직접 정할 사안
- [ ] `solo_entry_allowed`/`child_age_allowed` 이번 MVP 스코프 포함 여부
- [ ] `CALC_06`(노출등급 동점 보정) 구현 필요 여부
- [ ] 92_V5상세태깅60 62건 중 `review_status`(검수상태) 필드 존재 여부 및 값 확인 — R016 게이트 적용 대상 있는지
